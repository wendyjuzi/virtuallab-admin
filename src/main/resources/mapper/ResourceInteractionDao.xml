<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.virtuallab.resource.dao.ResourceInteractionDao">
    <insert id="insert" parameterType="com.edu.virtuallab.resource.model.ResourceInteraction">
        INSERT INTO resource_interaction (resource_id, user_id, interaction_type, content, rating, created_at, updated_at)
        VALUES (#{resourceId}, #{userId}, #{interactionType}, #{content}, #{rating}, NOW(), NOW())
    </insert>

    <select id="getInteractionsByResourceId" parameterType="long" resultType="com.edu.virtuallab.resource.model.ResourceInteraction">
        SELECT * FROM resource_interaction WHERE resource_id = #{resourceId}
    </select>

    <select id="selectById" parameterType="long" resultType="com.edu.virtuallab.resource.model.ResourceInteraction">
        SELECT id, resource_id as resourceId, user_id as userId, interaction_type as interactionType,
               content, rating, created_at as createdAt, updated_at as updatedAt
        FROM resource_interaction
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="com.edu.virtuallab.resource.model.ResourceInteraction">
        UPDATE resource_interaction
        SET interaction_type = #{interactionType},
            content = #{content},
            rating = #{rating},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM resource_interaction WHERE id = #{id}
    </delete>

    <!-- 统计特定类型的互动数量 -->
    <select id="getInteractionCount" parameterType="map" resultType="int">
        SELECT COUNT(*) FROM resource_interaction
        WHERE resource_id = #{resourceId} AND interaction_type = #{interactionType}
    </select>

    <select id="selectByResourceId" parameterType="long" resultType="com.edu.virtuallab.resource.model.ResourceInteraction">
        SELECT id, resource_id as resourceId, user_id as userId, interaction_type as interactionType,
               content, rating, created_at as createdAt, updated_at as updatedAt
        FROM resource_interaction
        WHERE resource_id = #{resourceId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByUserId" parameterType="string" resultType="com.edu.virtuallab.resource.model.ResourceInteraction">
        SELECT id, resource_id as resourceId, user_id as userId, interaction_type as interactionType,
               content, rating, created_at as createdAt, updated_at as updatedAt
        FROM resource_interaction
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByType" resultType="com.edu.virtuallab.resource.model.ResourceInteraction">
        SELECT id, resource_id as resourceId, user_id as userId, interaction_type as interactionType,
               content, rating, created_at as createdAt, updated_at as updatedAt
        FROM resource_interaction
        WHERE resource_id = #{resourceId} AND interaction_type = #{interactionType}
        ORDER BY created_at DESC
    </select>

    <select id="selectByUserAndResource" resultType="com.edu.virtuallab.resource.model.ResourceInteraction">
        SELECT id, resource_id as resourceId, user_id as userId, interaction_type as interactionType,
               content, rating, created_at as createdAt, updated_at as updatedAt
        FROM resource_interaction
        WHERE resource_id = #{resourceId} AND user_id = #{userId} AND interaction_type = #{interactionType}
        LIMIT 1
    </select>
</mapper>