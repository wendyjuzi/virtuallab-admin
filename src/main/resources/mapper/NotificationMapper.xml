<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.virtuallab.notification.dao.NotificationMapper">

    <resultMap id="BaseResultMap" type="com.edu.virtuallab.notification.model.Notification">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="type" property="type" jdbcType="VARCHAR"/>
        <result column="title" property="title" jdbcType="VARCHAR"/>
        <result column="content" property="content" jdbcType="VARCHAR"/>
        <result column="is_read" property="isRead" jdbcType="TINYINT"/>
        <result column="link" property="link" jdbcType="VARCHAR"/>
        <result column="related_id" property="relatedId" jdbcType="BIGINT"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 查询用户未读通知 -->
    <select id="findUnreadByUserId" resultType="com.edu.virtuallab.notification.model.Notification">
        SELECT * FROM notification
        WHERE user_id = #{userId} AND is_read = false
        ORDER BY created_at DESC
    </select>

    <!-- 标记通知为已读 -->
    <update id="markAsRead">
        UPDATE notification
        SET is_read = 1
        WHERE id = #{notificationId}
    </update>

    <!-- 其他SQL映射... -->

    <insert id="insert" parameterType="com.edu.virtuallab.notification.model.Notification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notification (
            user_id, type, title, content, is_read, link, related_id, created_at
        ) VALUES (
                     #{userId}, #{type}, #{title}, #{content}, #{isRead}, #{link}, #{relatedId}, #{createdAt}
                 )
    </insert>

    <select id="selectById" resultType="com.edu.virtuallab.notification.model.Notification">
        SELECT * FROM notification WHERE id = #{id}
    </select>

    <update id="update" parameterType="com.edu.virtuallab.notification.model.Notification">
        UPDATE notification SET
                                is_read = #{isRead}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM notification WHERE id = #{id}
    </delete>

    <update id="markAllAsRead">
        UPDATE notification
        SET is_read = true
        WHERE user_id = #{userId} AND is_read = false
    </update>
</mapper>