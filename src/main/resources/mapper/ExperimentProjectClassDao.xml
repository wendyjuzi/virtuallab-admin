<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.edu.virtuallab.experiment.dao.ExperimentProjectClassDao">

    <insert id="insert" parameterType="map">
        INSERT INTO experiment_project_class (project_id, class_id)
        VALUES (#{projectId}, #{classId})
    </insert>
    <select id="findStudentIdsByClassIds" resultType="java.lang.Long" parameterType="list">
        SELECT DISTINCT student_id
        FROM student_class
        WHERE class_id IN
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>




</mapper>
