<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.virtuallab.experiment.dao.ExperimentSceneDao">
    <insert id="insert" parameterType="com.edu.virtuallab.experiment.model.ExperimentScene">
        INSERT INTO experiment_scene (project_id, params, tasks)
        VALUES (#{projectId}, #{params}, #{tasks})
    </insert>

    <update id="update" parameterType="com.edu.virtuallab.experiment.model.ExperimentScene">
        UPDATE experiment_scene
        SET project_id = #{projectId},
            params = #{params},
            tasks = #{tasks}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM experiment_scene WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="long" resultType="com.edu.virtuallab.experiment.model.ExperimentScene">
        SELECT id, project_id, params, tasks
        FROM experiment_scene
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.edu.virtuallab.experiment.model.ExperimentScene">
        SELECT id, project_id, params, tasks
        FROM experiment_scene
    </select>
</mapper> 