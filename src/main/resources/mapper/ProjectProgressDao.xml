<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.virtuallab.project.dao.ProjectProgressDao">
    <insert id="insert" parameterType="com.edu.virtuallab.project.model.ProjectProgress">
        INSERT INTO project_progress (project_id, student_id, progress)
        VALUES (#{projectId}, #{studentId}, #{progress})
    </insert>
    <update id="update" parameterType="com.edu.virtuallab.project.model.ProjectProgress">
        UPDATE project_progress SET project_id=#{projectId}, student_id=#{studentId}, progress=#{progress}
        WHERE id=#{id}
    </update>
    <delete id="delete" parameterType="long">
        DELETE FROM project_progress WHERE id=#{id}
    </delete>
    <select id="selectById" parameterType="long" resultType="com.edu.virtuallab.project.model.ProjectProgress">
        SELECT * FROM project_progress WHERE id=#{id}
    </select>
    <select id="selectAll" resultType="com.edu.virtuallab.project.model.ProjectProgress">
        SELECT * FROM project_progress
    </select>
</mapper> 