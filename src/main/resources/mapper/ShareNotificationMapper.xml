<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.virtuallab.resource.mapper.ShareNotificationMapper">

    <resultMap id="ShareNotificationResultMap" type="com.edu.virtuallab.resource.dto.ShareNotification">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="message" column="message" />
        <result property="read" column="is_read" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="selectUnreadNotifications" resultMap="ShareNotificationResultMap">
        SELECT id, username, message, is_read, created_at
        FROM share_notification
        WHERE username = #{username} AND is_read = FALSE
        ORDER BY created_at DESC
    </select>

    <select id="selectAllNotifications" resultMap="ShareNotificationResultMap">
        SELECT id, username, message, is_read, created_at
        FROM share_notification
        WHERE username = #{username}
        ORDER BY created_at DESC
    </select>

    <update id="updateReadStatus">
        UPDATE share_notification
        SET is_read = #{read}
        WHERE id = #{notificationId}
    </update>

    <update id="updateAllReadStatus">
        UPDATE share_notification
        SET is_read = #{read}
        WHERE username = #{username}
    </update>

    <insert id="insertNotification" parameterType="com.edu.virtuallab.resource.dto.ShareNotification">
        INSERT INTO share_notification (username, message, is_read, created_at)
        VALUES (#{username}, #{message}, #{read}, #{createdAt})
    </insert>

</mapper> 