<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.virtuallab.project.dao.ProjectDao">
    <insert id="insert" parameterType="com.edu.virtuallab.project.model.Project">
        INSERT INTO project (name, description, status)
        VALUES (#{name}, #{description}, #{status})
    </insert>

    <update id="update" parameterType="com.edu.virtuallab.project.model.Project">
        UPDATE project
        SET name = #{name},
            description = #{description},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="long">
        DELETE FROM project WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="long" resultType="com.edu.virtuallab.project.model.Project">
        SELECT id, name, description, status
        FROM project
        WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.edu.virtuallab.project.model.Project">
        SELECT id, name, description, status
        FROM project
    </select>
</mapper> 